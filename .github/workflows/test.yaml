name: test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: lint
    steps:
      - name: setup node
        uses: actions/setup-node@v2
        with:
          node-version: "14"
          cache: yarn
      - name: checkout
        uses: actions/checkout@v2
      - name: lint
        run: yarn fix
